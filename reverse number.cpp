#include<bits/stdc++.h>
using name space std;
int main(){
	int n;
	cin>>n;
	int(reminder);
	int(reverse);
	while(n>0){
		reminder=number%10;
		reverse=reverse*10+reminder;
		num/=10
	}
	print("enter a number");
} 
